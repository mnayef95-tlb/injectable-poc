import 'package:injectable/injectable.dart';

const prodEnvironment = Environment(Environment.prod);
const devEnvironment = Environment(Environment.dev);
const testEnvironment = Environment(Environment.test);
