import 'package:flutter/material.dart';

class TopUpProvider extends StatelessWidget {
  const TopUpProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
